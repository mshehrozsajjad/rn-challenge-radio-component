# rn-challenge-radio-component

## Setting up  

clone this repository  
run `yarn install` or `npm install` into the cloned folder  
once the dependencies are installed, run the following command to run it on device or simulator.

```bash
expo start
```
